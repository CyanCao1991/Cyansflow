# 甲方PM工作流智能协作平台 - 部署指南

## 本地构建

项目已经构建完成，构建产物在 `dist/` 目录中。

### 快速部署方案

#### 方案1: Vercel（推荐）

1. **安装 Vercel CLI**
```bash
npm install -g vercel
```

2. **登录 Vercel**
```bash
vercel login
```

3. **部署**
```bash
cd /workspace
vercel --prod
```

部署成功后，Vercel会提供一个公网访问地址，例如：`https://your-project.vercel.app`

#### 方案2: Netlify

1. **安装 Netlify CLI**
```bash
npm install -g netlify-cli
```

2. **登录并部署**
```bash
netlify deploy --prod --dir=dist
```

#### 方案3: GitHub Pages

1. **创建 GitHub 仓库**

2. **安装 gh-pages**
```bash
npm install -D gh-pages
```

3. **添加部署脚本到 package.json**
```json
{
  "scripts": {
    "deploy": "gh-pages -d dist"
  }
}
```

4. **部署**
```bash
npm run deploy
```

#### 方案4: 自建服务器

将 `dist/` 目录下的所有文件复制到 Nginx/Apache 等 Web 服务器的网站根目录即可。

## 当前构建信息

- 构建时间: 2024年
- 构建版本: v1.0.0
- 构建产物: `/workspace/dist/`

## 功能特性

✅ 11个阶段工作流
✅ 关键活动和Checklist
✅ 自定义提示词模板
✅ 本地数据存储
✅ 响应式设计

## 技术支持

所有代码和文档都在 `/workspace/` 目录中，可根据需要自行部署到任何平台。
